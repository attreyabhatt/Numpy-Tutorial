import numpy as np

a = np.array([1,2,3,4])

# Element wise maths ( do same with other operations)

add = a + 2

# Adding two arrays
b = np.array([1,2,1,2])
add_array = a + b

# Take the cos/sin
c = np.sin(a)

## Reference docs - https://docs.scipy.org/doc/numpy/reference/routines.math.html

